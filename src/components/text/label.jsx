export default function Label({ label, className }) {
        return (
                <h4
                        className={className}
                >
                        {label}
                </h4>
        )
}